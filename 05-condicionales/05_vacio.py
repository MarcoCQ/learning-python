"""

    len(): Esto indica la cantidad de caracteres que tiene un texto

"""

text = input("Ingresa un texto:")

print(len(text)) # Cuentas cada letra del texto

if not text:
    print("No ingresaste texto")
